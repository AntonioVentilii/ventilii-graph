# Ignore files for PNPM, NPM and YARN
pnpm-lock.yaml
package-lock.json
yarn.lock

# Owned by release-please — let it control the formatting. The manifest is
# also ignored because release-please writes 2-space JSON while this repo's
# prettier uses tabs (useTabs), so it would otherwise fail the lint check.
/CHANGELOG.md
/.release-please-manifest.json
